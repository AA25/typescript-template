# typescript-template
Typescript template with Jest for quickly writing testable typescript code

## Commands to run
* Start script
  * Use node version 16
  * `npm install`
    * `npx ts-node index.ts`


* Run test
  * `npx jest`
